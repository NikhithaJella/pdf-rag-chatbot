# URL of the deployed backend (FastAPI server)
# In development this defaults to http://localhost:8000 — no need to set it locally.
# In production, set this to your Render/Railway/Fly.io backend URL.
VITE_API_BASE_URL=https://your-backend.onrender.com
